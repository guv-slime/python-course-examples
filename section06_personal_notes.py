# String Library - Popular Methods

'''
str.capitalize()                        str.replace(old, new[, count])
str.center(width[, start[, end]])       str.lower()
str.endswith(suffix[, start[, end]])    str.rstrip([chars]) or lstrip meaning left
str.find(sub[, start[, end]])           str.strip([chars])  removes whitespace on both sides
str.1strip([chars])                     str.upper()
'''

# in Python 3, all strings are Unicode (i.e. other languages' character sets are treated equally

# format operator, %, is confusing but can be useful I suppose
